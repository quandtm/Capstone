<common:LayoutAwarePage
    x:Class="Capstone.Editor.Views.GamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:Capstone.Editor.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="using:Capstone.Editor.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:GameViewModel, IsDesignTimeCreatable=True}"
    mc:Ignorable="d">
    
    <SwapChainBackgroundPanel x:Name="swapPanel" Loaded="swapPanel_Loaded">
        <TextBlock Margin="0,50,48,0" HorizontalAlignment="Right" VerticalAlignment="Top" FontFamily="High Tower Text" Foreground="#FF2E2E2E">
        	<Run Text="{Binding Health}" FontSize="50"/>
        	<Run Text=" HP" FontSize="18"/>
        </TextBlock>
        <TextBlock Margin="0,50,50,0" HorizontalAlignment="Right" VerticalAlignment="Top" FontFamily="High Tower Text">
        	<Run Text="{Binding Health}" FontSize="48"/>
        	<Run Text=" HP" Foreground="#FFE4E4E4" FontSize="16"/>
        </TextBlock>
    </SwapChainBackgroundPanel>

</common:LayoutAwarePage>
