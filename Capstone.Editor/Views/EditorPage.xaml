<common:LayoutAwarePage
    x:Class="Capstone.Editor.Views.EditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:Capstone.Editor.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Capstone.Editor.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewModels:EditorViewModel, IsDesignTimeCreatable=True}"
    mc:Ignorable="d">
	<common:LayoutAwarePage.Resources>
		<common:Inverter x:Key="Inverter"/>
		<common:InvertBoolVisibility x:Key="InverseBoolVisibility"/>
        <common:BoolVisibility x:Key="BoolVisibility" />
		<DataTemplate x:Key="SpritePreviewTemplate">
			<Grid Width="100" Height="150">
				<Grid.RowDefinitions>
					<RowDefinition Height="100"/>
					<RowDefinition Height="50"/>
				</Grid.RowDefinitions>
				<Grid.Background>
					<SolidColorBrush Color="{StaticResource LightGreyBackground}"/>
				</Grid.Background>
				<Image HorizontalAlignment="Center" Height="100" VerticalAlignment="Top" Width="100" Source="{Binding PreviewImage, Mode=OneWay}" Stretch="None"/>
				<TextBlock TextWrapping="Wrap" Text="{Binding PreviewLabel, Mode=OneWay}" VerticalAlignment="Top" Margin="5,0" TextAlignment="Center" Grid.Row="1" FontSize="13.333"/>
				<TextBlock TextWrapping="Wrap" VerticalAlignment="Top" Grid.Row="1" FontSize="13.333" Margin="5,30,5,0" HorizontalAlignment="Center">
					<Run Text="$"/>
					<Run Text="{Binding Cost}"/>
				</TextBlock>
			</Grid>
		</DataTemplate>
		<DataTemplate x:Key="ObjectiveItemTemplate">
			<Grid Background="#19000000" IsHitTestVisible="False" Visibility="{Binding IsComplete, Converter={StaticResource InverseBoolVisibility}, Mode=OneWay}">
				<TextBlock TextWrapping="Wrap" VerticalAlignment="Top" FontSize="16" Margin="5,2" IsHitTestVisible="False">
					<Run Text="-"/>
					<Run Text="{Binding Description}"/>
					<Run Text="("/>
					<Run Text="{Binding Count}"/>
					<Run Text="/"/>
					<Run Text="{Binding Total}"/>
					<Run Text=")"/>
				</TextBlock>
			</Grid>
		</DataTemplate>
	</common:LayoutAwarePage.Resources>
    
	<common:LayoutAwarePage.BottomAppBar>
		<AppBar>
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<StackPanel Orientation="Horizontal"/>
				<StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
					<Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Style="{StaticResource SaveAppBarButtonStyle}"/>
				</StackPanel>
			</Grid>
		</AppBar>
	</common:LayoutAwarePage.BottomAppBar>

    <SwapChainBackgroundPanel x:Name="swapPanel" Loaded="HandleLoaded" PointerReleased="swapPanel_PointerReleased">
    	<SwapChainBackgroundPanel.ColumnDefinitions>
    		<ColumnDefinition Width="80"/>
    		<ColumnDefinition/>
    		<ColumnDefinition Width="326"/>
    	</SwapChainBackgroundPanel.ColumnDefinitions>
    	<SwapChainBackgroundPanel.RowDefinitions>
    		<RowDefinition Height="40"/>
    		<RowDefinition Height="45*"/>
    		<RowDefinition Height="46*"/>
    	</SwapChainBackgroundPanel.RowDefinitions>
    	<Grid x:Name="infoPanel" Grid.ColumnSpan="3" Background="#FF2C2C2C">
    		<TextBlock HorizontalAlignment="Left" Margin="20,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" Style="{StaticResource BasicTextStyle}">
    			<Run Text="$"/>
    			<Run Text="{Binding Money}"/>
    		</TextBlock>
    		<Button Content="Object Editor" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,20,0"/>
    		<Button Content="Toggle Event Editor" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,170,0" Click="ToggleEventEditor"/>
    	</Grid>
    	<StackPanel x:Name="toolPanel" Grid.Row="1" Margin="0" Grid.RowSpan="2">
    		<StackPanel.Background>
    			<SolidColorBrush Color="{StaticResource DarkGreyBackground}"/>
    		</StackPanel.Background>
    		<Button x:Name="btnPan" Content="Pan" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="80" Click="ChangeToolPan" IsEnabled="{Binding IsPanTool, Converter={StaticResource Inverter}, Mode=OneWay}"/>
    		<Button x:Name="btnSelect" Content="Select" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="80" Click="ChangeToolSelect" Margin="0,20,0,0" IsEnabled="{Binding IsSelectTool, Converter={StaticResource Inverter}, Mode=OneWay}"/>
    		<Button x:Name="btnBuild" Content="Place" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="80" Margin="0" Click="ChangeToolBuild" IsEnabled="{Binding IsBuildTool, Converter={StaticResource Inverter}, Mode=OneWay}"/>
    	</StackPanel>
    	<Grid x:Name="inspectorPanel" Grid.Row="1" Grid.Column="2" Visibility="{Binding EventEditorVisible, Converter={StaticResource InverseBoolVisibility}}">
    		<Grid.Background>
    			<SolidColorBrush Color="{StaticResource DarkGreyBackground}"/>
    		</Grid.Background>
    		<Grid.RowDefinitions>
    			<RowDefinition Height="40"/>
    			<RowDefinition/>
    		</Grid.RowDefinitions>
    		<TextBlock x:Name="inspectorLabel" HorizontalAlignment="Left" TextWrapping="Wrap" Text="Inspector" VerticalAlignment="Top" Style="{StaticResource GroupHeaderTextStyle}" Margin="20,0,0,0"/>
    		<ScrollViewer Margin="0" Grid.Row="1"/>
    	</Grid>
    	<Grid x:Name="constructionPanel" Margin="0" Grid.Column="2" Grid.Row="2" Visibility="{Binding EventEditorVisible, Converter={StaticResource InverseBoolVisibility}}">
    		<Grid.RowDefinitions>
    			<RowDefinition Height="40"/>
    			<RowDefinition/>
    		</Grid.RowDefinitions>
    		<Grid.Background>
    			<SolidColorBrush Color="{StaticResource DarkGreyBackground}"/>
    		</Grid.Background>
    		<Button x:Name="btnBuildMenu" Content="Objects" HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Center" Style="{StaticResource TextButtonStyle}" Click="OpenBuildMode"/>
    		<Button x:Name="btnLandscapeMenu" Content="Landscape" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource TextButtonStyle}" Margin="100,0,0,0" Click="OpenLandscapeMode"/>
    		<Grid Grid.Row="1">
    			<GridView ItemsSource="{Binding Objects}" ItemTemplate="{StaticResource SpritePreviewTemplate}" SelectedItem="{Binding SelectedObject, Mode=TwoWay}" />
    		</Grid>
    	</Grid>
    	<Grid x:Name="eventPanel" Grid.Column="2" Margin="0" Grid.Row="1" Grid.RowSpan="2" Visibility="{Binding EventEditorVisible, Converter={StaticResource BoolVisibility}}">
    		<Grid.Background>
    			<SolidColorBrush Color="{StaticResource DarkGreyBackground}"/>
    		</Grid.Background>
    	</Grid>
    	<ListView Grid.Column="1" HorizontalAlignment="Left" Grid.Row="1" Width="300" Grid.RowSpan="2" IsHitTestVisible="False" SelectionMode="None" IsSwipeEnabled="False" ItemTemplate="{StaticResource ObjectiveItemTemplate}" ItemsSource="{Binding Objectives}"/>
    </SwapChainBackgroundPanel>

</common:LayoutAwarePage>
